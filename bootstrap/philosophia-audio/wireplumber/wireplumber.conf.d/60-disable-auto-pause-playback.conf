# Disable WirePlumber's MPRIS pause-on-output-removal policy on philosophia.
# This stops Bluetooth headphone removal from pausing media players.
wireplumber.settings = {
  linking.pause-playback = false
}
